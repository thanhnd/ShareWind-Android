package com.sharewind.app.ui.theme

import androidx.compose.ui.graphics.Color

// Primary — Cyan Blue (Wind / Energy)
val Primary = Color(0xFF00B4D8)
val PrimaryHover = Color(0xFF0096C7)
val PrimaryLight = Color(0xFFE0FBFC)

// Secondary — Trust / Emphasis
val Secondary = Color(0xFF0077B6)

// Accent — Highlight, Pending, Rating
val Accent = Color(0xFFFFB703)

// Semantic
val Success = Color(0xFF2A9D8F)
val Warning = Color(0xFFE9C46A)
val Error = Color(0xFFE76F51)

// Neutral
val Neutral900 = Color(0xFF212529)
val Neutral600 = Color(0xFF6C757D)
val Neutral300 = Color(0xFFDEE2E6)
val Neutral100 = Color(0xFFF8F9FA)
val Background = Color(0xFFFFFFFF)

// Legacy aliases (for gradual migration)
val BluePrimary = Primary
val BlueSecondary = Secondary
val BlueTertiary = PrimaryLight
val DarkGrey = Neutral900
val LightGrey = Neutral100
val ErrorRed = Error
